name: Package Server

on:
  pull_request:
    branches:
      - main
    paths:
      - "server/**"
      - ".github/workflows/build.server.yml"
      - ".github/workflows/package.server.yml"
  workflow_call:
    inputs:
      version:
        required: false
        type: string
        default: 'latest'

jobs:
  build:
    uses: ./.github/workflows/build.server.yml

  package:
    runs-on: ubuntu-latest
    needs: build
    defaults:
      run:
        working-directory: server

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          package_json_file: server/package.json

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"
          cache-dependency-path: server/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install

      - name: Set package.json version
        if: ${{ inputs.version && inputs.version != 'latest' }}
        run: pnpm version ${{ inputs.version }} --no-git-tag-version

      - name: Build Docker Image
        run: docker build -t server:latest .

      - name: Save Docker Image
        run: docker save server:latest -o server-${{ inputs.version || 'latest' }}.tar

      - name: Upload Docker Image
        uses: actions/upload-artifact@v6
        with:
          name: docker-image-${{ inputs.version || 'latest' }}
          path: server/server-${{ inputs.version || 'latest' }}.tar
          if-no-files-found: error
