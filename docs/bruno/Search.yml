info:
  name: Search
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/search?q=general intelligence&sessionId=1"
  params:
    - name: q
      value: general intelligence
      type: query
    - name: page
      value: "2"
      type: query
      disabled: true
    - name: sessionId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
