info:
  name: Search
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/search?q=ai"
  params:
    - name: q
      value: ai
      type: query
    - name: page
      value: "2"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
