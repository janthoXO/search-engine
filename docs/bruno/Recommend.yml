info:
  name: Recommend
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/recommend?sessionId=1"
  params:
    - name: sessionId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
