info:
  name: Suggest
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/suggest?q=ai"
  params:
    - name: q
      value: ai
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
